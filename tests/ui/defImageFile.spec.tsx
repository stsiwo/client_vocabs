//import * as React from 'react';
//import { mount } from 'enzyme';
//import * as sinon from 'sinon';
//import { SinonSpy } from 'sinon';
//import DefImageFile from '../../src/representationals/business/SearchImageModal/DefImageFile';
//import configureMockStore from 'redux-mock-store';
//import thunk from 'redux-thunk';
//import { initialNormalizedState } from '../../src/state/index';
//import { ProviderAndThemeWrapperHOC } from '../helper/ProviderAndThemeWrapperHOC';

//interface ImageIF {
  //name: string;
  //src: string;
//}

//interface Props {
  //className?: string;
  //items: ImageIF[]; 
  //updateDefImageClick: ( id: string, nextImage: string ) => void;
  //defId: string;
//}

describe('DefImageFile', function() {
  //const mockStore = configureMockStore([ thunk ]);
  it('should invoke the event handler is called when word image is change', function() {
    //let store = mockStore(initialNormalizedState);

    // props spy 
    //const updateDefImageClickSpy: SinonSpy = sinon.spy();
    //const updateDefImageClickSpy: SinonSpy = sinon.spy();
 
    ////const ContextHOC = ProviderAndThemeWrapperHOC<Props>(DefImageFile, store);
     ////const wrapper = mount(
        ////<ContextHOC items={ [ imageItemDummy ] } defId={ '1' } updateDefImageClick={ updateDefImageClickSpy } />
     ////) 
     //const wrapper = mount(
        //<DefImageFile image={ 'image' } defId={ '1' } updateDefImageClick={ updateDefImageClickSpy } />
     //);

     //console.log(wrapper.debug()); 
    
     //wrapper.find("img").first().simulate('click');

     //sinon.assert.calledOnce(updateDefImageClickSpy);
  })
})




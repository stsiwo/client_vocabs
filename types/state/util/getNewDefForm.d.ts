import { IDef } from '../../domains/def';
/**
 * new word esp for add new word form when new icon is clicked
 **/
declare const getNewDefForm: (id: string) => IDef;
export default getNewDefForm;
